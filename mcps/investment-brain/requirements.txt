# Investment Brain — Requirements
# Install: pip install -r requirements.txt
#
# Audit log (Ticket L7a):
# - Removed `requests` (unused — no `import requests` anywhere in the codebase).
# - Added `mcp` (used by mcp_wrapper.py for MCP server side).
# - All deps pinned to exact versions to ensure reproducible installs.

# Data fetching — fallback path when MCP server is not available.
yfinance==0.2.50

# Numerical libraries — used by data_fetcher and downstream by yfinance/pandas.
pandas==2.2.3
numpy==2.1.3

# Web UI — FastAPI + Uvicorn power web_app.py (browser dashboard at :8000).
fastapi==0.115.5
uvicorn==0.32.1
pydantic==2.10.3

# MCP server library — used by mcp_wrapper.py to expose investment-brain
# as an MCP server that Claude Desktop / Claude Code can call directly.
mcp==1.1.2
