# Skill: Expo Build

Build and publish Expo app.

## Commands

- `npx expo start --lan` — Start dev server on LAN
- `npx expo run:android` — Build Android development version
- `npx expo run:ios` — Build iOS development version
- `eas build --platform all` — Production build via EAS
- `eas submit --platform all` — Submit to app stores
